{
  "build": {
    "builder": "nixpacks",
    "config": {
      "scripts": {
        "build": "pip install -r requirements.txt"
      }
    }
  },
  "deploy": {
    "start": "gunicorn main:app -b 0.0.0.0:$PORT -t 120 --workers 2"
  }
}
